<section class="section">
	<slot />
</section>

<style lang="scss">
	section.section {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		margin-inline: var(--layout-inline);
	}
</style>
